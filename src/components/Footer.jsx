export default function Footer(){
  return (
    <footer className="mt-20 border-t border-line">
      <div className="container-h py-10 text-sm text-white/60">
        Â&copy; {new Date().getFullYear()} VenueHub — built for entertainers & venues.
      </div>
    </footer>
  );
}


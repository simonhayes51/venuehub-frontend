export default function ShortlistButton({id,type}){const k="vh_shortlist";const d=JSON.parse(localStorage.getItem(k)||"[]");const e=d.some(x=>x.id===id&&x.type===type);const t=()=>{let a=JSON.parse(localStorage.getItem(k)||"[]");if(e)a=a.filter(x=>!(x.id===id&&x.type===type));else a.push({id,type,at:Date.now()});localStorage.setItem(k,JSON.stringify(a));location.reload()};return <button className="btn-ghost text-sm" onClick={t}>{e?"Remove from shortlist":"Save to shortlist"}</button>}